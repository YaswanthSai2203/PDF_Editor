import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    template: "%s | DocFlow",
    default: "DocFlow — Professional PDF Editor",
  },
  description:
    "A premium PDF editor for professionals. Annotate, sign, edit, organize, and collaborate on PDFs with ease.",
  keywords: ["PDF editor", "PDF annotator", "e-signature", "PDF tools"],
  authors: [{ name: "DocFlow" }],
  creator: "DocFlow",
  metadataBase: new URL(
    process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000"
  ),
  openGraph: {
    type: "website",
    title: "DocFlow — Professional PDF Editor",
    description:
      "A premium PDF editor for professionals. Annotate, sign, edit, organize, and collaborate on PDFs with ease.",
    siteName: "DocFlow",
  },
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#ffffff" },
    { media: "(prefers-color-scheme: dark)", color: "#0f172a" },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
